<%= form_with model: @sick_leave, local: true do |form| %>
    <% if @sick_leave.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@sick_leave.errors.count, "error") %> prohibited this sick leave from being saved:</h2>
  
        <ul>
          <% @sick_leave.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :start_date %>
      <%= form.date_select :start_date %>
    </div>
  
    <div class="field">
      <%= form.label :end_date %>
      <%= form.date_select :end_date %>
    </div>
  
    <div class="field">
      <%= form.label :reason %>
      <%= form.text_area :reason %>
    </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
  